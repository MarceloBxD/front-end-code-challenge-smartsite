import React from "react";

type FoundUnitBoxProps = {
  status: string;
  location: string;
  address: string;
  state: string;
};

export const FoundUnitBox = ({
  status,
  location,
  address,
}: FoundUnitBoxProps) => {
  return <div>FoundUnitBox</div>;
};
